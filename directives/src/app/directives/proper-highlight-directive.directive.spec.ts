import { ProperHighlightDirectiveDirective } from './proper-highlight-directive.directive';

describe('ProperHighlightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ProperHighlightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
