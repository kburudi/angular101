import { StracturalDirective } from './stractural.directive';

describe('StracturalDirective', () => {
  it('should create an instance', () => {
    const directive = new StracturalDirective();
    expect(directive).toBeTruthy();
  });
});
